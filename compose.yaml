# Copyright 2026 上海如静知华信息科技有限公司 · https://www.zhuatech.cn/
services:
  mysql:
    image: mysql:8.4
    environment:
      MYSQL_DATABASE: ${MYSQL_DATABASE:-zhuatech_retail}
      MYSQL_USER: ${MYSQL_USER:-zhuatech}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-zhuatech_dev_password}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-zhuatech_root_password}
    volumes: [his_mysql_data:/var/lib/mysql]
    healthcheck: {test: ["CMD", "mysqladmin", "ping", "-h", "localhost"], interval: 10s, timeout: 5s, retries: 10}
  backend:
    build: ./backend
    environment:
      DB_URL: jdbc:mysql://mysql:3306/${MYSQL_DATABASE:-zhuatech_retail}?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
      DB_USERNAME: ${MYSQL_USER:-zhuatech}
      DB_PASSWORD: ${MYSQL_PASSWORD:-zhuatech_dev_password}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-admin123}
      OPERATOR_PASSWORD: ${OPERATOR_PASSWORD:-operator123}
    depends_on: {mysql: {condition: service_healthy}}
    ports: ["${SERVER_PORT:-8080}:8080"]
  frontend:
    build: ./frontend
    depends_on: [backend]
    ports: ["${WEB_PORT:-8090}:80"]
volumes:
  his_mysql_data:
